<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpenTK_compute_conestepmap"
        xmlns:ViewModel="clr-namespace:OpenTK_compute_conestepmap.ViewModel" x:Name="window_view" x:Class="OpenTK_compute_conestepmap.View.OpenTK_View"
        mc:Ignorable="d"
        Title="OpenTK WPF compute cone step map" Height="450" Width="400">
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,0" StartPoint="0.5,1">
            <GradientStop Color="Gray"/>
            <GradientStop Color="White" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <Window.DataContext>
        <ViewModel:OpenTK_ViewModel/>
    </Window.DataContext>
    <Grid x:Name="grid">
        <ContentControl x:Name="host" Margin="10" Grid.ColumnSpan="2" Content="{Binding GLHostControl}" />
    </Grid>
</Window>
