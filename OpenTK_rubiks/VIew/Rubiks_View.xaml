<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpenTK_rubiks"
        xmlns:ViewModel="clr-namespace:OpenTK_rubiks.ViewModel" x:Name="window_view" x:Class="OpenTK_rubiks.View.OpenTK_View"
        mc:Ignorable="d"
        Title="OpenTK View" Height="500" Width="500">
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,0" StartPoint="0.5,1">
            <GradientStop Color="Gray"/>
            <GradientStop Color="White" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <Window.DataContext>
        <ViewModel:Rubiks_ViewModel/>
    </Window.DataContext>
    <Grid x:Name="grid">
        <ContentControl x:Name="host" Margin="0" Grid.ColumnSpan="2" Content="{Binding GLHostControl}" />
    </Grid>
</Window>
