<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpenTK_compute_raytracing"
        xmlns:ViewModel="clr-namespace:OpenTK_compute_raytracing.ViewModel" x:Class="OpenTK_compute_raytracing.View.RayTracingView"
        mc:Ignorable="d"
        Title="OpenTK Raytracing" Height="500" Width="500">
    <Window.DataContext>
        <ViewModel:RayTracing_ModelView/>
    </Window.DataContext>
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,0" StartPoint="0.5,1">
            <GradientStop Color="Gray"/>
            <GradientStop Color="White" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid x:Name="grid">
        <ContentControl x:Name="host" Margin="10" Grid.ColumnSpan="2" Content="{Binding GLHostControl}" />
    </Grid>
</Window>
